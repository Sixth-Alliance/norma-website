name = "your-nextjs-app"
# The folder containing your compiled static assets/worker
pages_build_output_dir = "out" 

compatibility_date = "2026-05-19"
compatibility_flags = [ "nodejs_compat" ]

# Adding this empty block signals to Cloudflare that it should 
# merge these with the dashboard environment variables.
[vars]